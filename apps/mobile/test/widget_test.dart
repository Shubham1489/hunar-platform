import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should build without errors', (WidgetTester tester) async {
    // Placeholder until full Riverpod test setup
    expect(1 + 1, equals(2));
  });
}
